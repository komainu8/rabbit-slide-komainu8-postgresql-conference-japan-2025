デモシナリオ：

1. キーワード検索結果とセマンティックサーチの検索結果を比較する。

以下を確認できるような結果を取得するSQLを実行して見せる。

- キーワード検索では、検索漏れが発生するけど正確なレコードが取得できること
- セマンティックサーチでは、検索漏れは発生しにくいけど、ノイズが増えること

2. pgvectorを使った場合のデータ登録と検索

以下を実際にやる。PGroongaでも同様のことを実施してやり方の違いを示す。
なるべく、PGroongaと同じような結果がでる検索クエリーが望ましい。
検索クエリーはpgvectorとPGroongaで共通。

- 外部サービスによるテキストデータ->ベクトルデータへの変換
- 変換したデータをINSERTする
- セマンティックサーチを実行する

3. PGroongaを使った場合のデータ登録と検索

以下を実際にやる。pgvectorでも同様のことを実施してやり方の違いを示す。
なるべく、pgvectorと同じような結果がでる検索クエリーが望ましい。

- テキストデータをINSERTする
- INSERTしたテキストデータがベクトルデータに変換されているのを確認する
- セマンティックサーチを実行する

4. FAQ検索

できればRedmine full text searchを使ってRedmien上でセマンティックサーチを使えることを示したい。
ただ、改良が必要だったり、適当なデータが存在しない場合もあるので、間に合わない場合はSQLでそれっぽい質問をする
検索クエリーを作って結果を見せる形にする。

- 過去の事例を検索するようなクエリーを実行
- 実行結果を見せて、FAQ検索のようなケースで使えることを示す。
